# Snapshots — Oriol

Raw NHS-SIM JSON, one directory per pull: `snapshot/<timestamp>/`.

**Contents are gitignored** — they are large and regenerable, and the calibrated parameters
(which are what the rest of the repo needs) live in `fixtures/` and `docs/parameters.md` instead.

Write raw. Do not parse in the fetch.
