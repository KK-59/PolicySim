/**
 * Rank plans: hard safety fails disqualify -> robustness across the three worlds ->
 * time-to-safe -> patient burden -> staff burden.
 * A plan that only wins in the optimistic world is not a recommendation.
 * Produces "why plan 4 won" and "what plan 2 would have cost" from the event log.
 * OWNER: Kaavya (mechanism) + Albert (constraints).
 */

export {};
