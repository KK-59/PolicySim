/**
 * Snapshot -> parameters. Resource counts to arrival rates, session config to capacities,
 * status transition counts to routing probabilities. Every number handed to Kaavya carries
 * a source tag.
 * OWNER: Oriol. Under outage, fall back to handbook delays + literature (PRD §4.4).
 */

export {};
