/**
 * Commitments + retrieved values -> a Params object with a source tag on every field.
 * Clamp to physical bounds. Anything neither source covers is tagged "assumed" and FLAGGED.
 * OWNER: Oriol. Consumed by: Elsa's extracted-parameter view.
 */

export {};
