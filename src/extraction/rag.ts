/**
 * Stage 2: retrieval over corpus/ for every parameter the document does not pin down.
 * Returns a value AND a range -> tagged "literature". Those ranges become the three worlds.
 * OWNER: Albert (corpus + chunking), Oriol (wiring).
 */

export {};
