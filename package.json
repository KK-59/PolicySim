{
  "name": "policy-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Every policy runs in three worlds. Team 14 \u2014 OpenAI x Anima Healthtech Hackathon.",
  "type": "module",
  "scripts": {
    "dev": "echo 'TODO (Elsa): wire up Vite dev server' && exit 1",
    "build": "echo 'TODO (Elsa): wire up build' && exit 1",
    "test": "node tests/verify.ts",
    "typecheck": "tsc --noEmit",
    "snapshot": "echo 'TODO (Oriol): node scripts/snapshot.mjs' && exit 1",
    "grid": "node scripts/build-grid.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
