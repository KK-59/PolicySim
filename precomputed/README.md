# Precomputed bundle — Kaavya

The demo safety net. Nothing can crash on stage because the UI is a lookup.

`grid.json` — 3 levers × 10 positions, each with its three-world bands. Built by
`scripts/build-grid.mjs`. **Ship this before polish, not after.**

If extraction is flaky on the day, the pre-extracted parameter sets for the two demo documents
also live here. The upload gesture still happens on stage; only the live inference is dropped.
Declare it if asked.
